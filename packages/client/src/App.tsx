import { GYM_NAME } from "@jeyos/shared";
import "@jeyos/domain";

// Placeholder page. U3 replaces it with the app shell and the owner and staff screens.
export function App() {
  return (
    <main>
      <h1>{GYM_NAME}</h1>
      <p>Gym management system, under construction.</p>
    </main>
  );
}
