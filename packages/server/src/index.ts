// The Express app that runs on the gym PC. U2 replaces this placeholder.
import { GYM_NAME } from "@jeyos/shared";

console.log(`${GYM_NAME} server placeholder, running on Node ${process.version}`);
